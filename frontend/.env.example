# 后端 API 地址 (生产 GH Pages 必填, 走 repo Secret VITE_API_BASE).
# 末尾不带 /. 例: https://yourname-ai-chatbot.hf.space/api/v1
VITE_API_BASE=

# GitHub 仓库名 (用于 base path, 必须与 GH 仓库名**大小写完全一致**).
# 例: AI-Chatbot (注意大写 A C). 通常**不要**设这个, 让 vite.config.ts 从
# GITHUB_REPOSITORY 自动取, 以免大小写不一致.
# VITE_REPO_NAME=AI-Chatbot
